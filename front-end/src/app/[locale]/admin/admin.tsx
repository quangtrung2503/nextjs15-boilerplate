"use client"
import { default as CommonStyles } from "@/components/common"

interface UserData {
  id: number;
  name: string;
  email: string;
  role: string;
}
const Admin = () => {
  
  return (
    <CommonStyles.Box>
    </CommonStyles.Box>
  );
}

export default Admin;